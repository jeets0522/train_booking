# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Java-based train booking application. This project is in its initial setup phase — no source code, build files, or configuration exist yet.

Update this file once the project is scaffolded with:
- Build commands (e.g., `mvn clean install`, `./gradlew build`)
- How to run the application
- How to run tests (all tests and single tests)
- Architecture overview once the package structure and layers are established
